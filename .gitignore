# This .gitignore file should be placed at the root of your Unity project directory
#
# Get latest from https://github.com/github/gitignore/blob/master/Unity.gitignore
#
# Modified to fit with the structure of my project
final project assets/
GPU Flocking/[Ll]ibrary/
GPU Flocking/[Tt]emp/
GPU Flocking/[Oo]bj/
GPU Flocking/[Bb]uild/
GPU Flocking/[Bb]uilds/
GPU Flocking/[Ll]ogs/
GPU Flocking/[Uu]ser[Ss]ettings/

# MemoryCaptures can get excessive in size.
# They also could contain extremely sensitive data
GPU Flocking/[Mm]emoryCaptures/

# Asset meta data should only be ignored when the corresponding asset is also ignored
!/[Aa]ssets/**/*.meta

# Uncomment this line if you wish to ignore the asset store tools plugin
# GPU Flocking/[Aa]ssets/AssetStoreTools*

# Autogenerated Jetbrains Rider plugin
GPU Flocking/[Aa]ssets/Plugins/Editor/JetBrains*

# Visual Studio cache directory
GPU Flocking/.vs/

# Gradle cache directory
GPU Flocking/.gradle/

# Autogenerated VS/MD/Consulo solution and project files
GPU Flocking/ExportedObj/
GPU Flocking/.consulo/
GPU Flocking/*.csproj
GPU Flocking/*.unityproj
GPU Flocking/*.sln
GPU Flocking/*.suo
GPU Flocking/*.tmp
GPU Flocking/*.user
GPU Flocking/*.userprefs
GPU Flocking/*.pidb
GPU Flocking/*.booproj
GPU Flocking/*.svd
GPU Flocking/*.pdb
GPU Flocking/*.mdb
GPU Flocking/*.opendb
GPU Flocking/*.VC.db

# Unity3D generated meta files
GPU Flocking/*.pidb.meta
GPU Flocking/*.pdb.meta
GPU Flocking/*.mdb.meta

# Unity3D generated file on crash reports
GPU Flocking/sysinfo.txt

# Builds
GPU Flocking/*.apk
GPU Flocking/*.aab
GPU Flocking/*.unitypackage

# Crashlytics generated file
GPU Flocking/crashlytics-build.properties

# Packed Addressables
GPU Flocking/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*

# Temporary auto-generated Android Assets
GPU Flocking/[Aa]ssets/[Ss]treamingAssets/aa.meta
GPU Flocking/[Aa]ssets/[Ss]treamingAssets/aa/*